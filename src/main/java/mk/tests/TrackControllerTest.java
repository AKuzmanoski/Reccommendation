package mk.tests;

public class TrackControllerTest {
    /*@Test
    public void shouldDisplayTrack() {
        Track track = new Track("123", "Nothing Else Matters", "http://youtube.com/NothingElseMatters", "Metallica");
        track.setEntityId(1L);
        TrackService service = Mockito.mock(TrackService.class);
        Mockito.when(service.getTrackById(1L)).thenReturn(track);

        TrackController controller = new TrackController(service);

        Map<String, Object> model = new HashMap<String, Object>();
        String viewName = controller.showTrack(model, 1L);

        Assert.assertEquals(viewName, "song");

        Assert.assertSame(track, model.get("track"));
    }*/
}
